# Backend agents — Node scripts, not part of the React build
# (this also covers agents/**/data/, so no need for a separate **/data/ rule)
agents/

# Tooling that doesn't belong in deployment
make-blueprints/
server/
testing/
_patches/
_scripts/

# Old standalone build artifact
arvo-os.html

# Standard
node_modules/
build/
.env*
.git
.github
