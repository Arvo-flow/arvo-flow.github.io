name: Probe Bolagsverket
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/probe-bolagsverket.mjs'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Probe
        run: |
          mkdir -p ops
          node scripts/probe-bolagsverket.mjs | tee ops/probe-bolagsverket.txt
      - name: Commit
        if: always()
        continue-on-error: true
        run: |
          git config user.email "bot@arvoflow.se"
          git config user.name "Arvo Probe Bot"
          git pull --rebase origin main || true
          git add ops/probe-bolagsverket.txt
          git commit -m "ops: probe-bolagsverket [skip ci]" || echo "inget"
          git push origin HEAD:main || echo "push miss"
