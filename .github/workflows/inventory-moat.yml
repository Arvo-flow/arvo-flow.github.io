name: Inventory moat
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  inv:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - name: Inventory
        run: |
          # pipefail: utan den är stegets exit-kod tee:s (alltid 0) — en död sond rapporterar framgång.
          set -o pipefail
          mkdir -p ops
          node scripts/inventory-moat.mjs | tee ops/inventory-moat.txt
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
      - name: Commit
        # ⚠️ ENDAST FRÅN main (2026-09-10). Utan villkoret pushar ett workflow_dispatch på en
        #    feature branch DEN BRANCHEN till main — tyst (`|| echo`), förbi granskningsgrinden.
        if: always() && github.ref == 'refs/heads/main'
        continue-on-error: true
        run: |
          git config user.email "bot@arvoflow.se"
          git config user.name "Arvo Inv Bot"
          git pull --rebase origin main || true
          git add ops/inventory-moat.txt
          git commit -m "ops: inventory-moat [skip ci]" || echo "inget"
          git push origin HEAD:main || echo "push miss"
